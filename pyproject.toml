[project]
name = "comfyui-wanmove-path-animator"
description = "Interactive path animator with visual editor for creating animated shapes that follow user-drawn paths. Wan Move compatible for stable video generation."
version = "1.0.6"
license = { file = "LICENSE.txt" }
dependencies = [
    "Pillow>=9.0.0",
    "numpy>=1.20.0"
]

[project.urls]
Repository = "https://github.com/brbbbq/ComfyUI-WanMove-Path-Animator"

[tool.comfy]
PublisherId = "brbbbq"
DisplayName = "WanMove Path Animator"
Icon = ""
